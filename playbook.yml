---
- name: Deployment of Genre Classification application
  hosts: all
  become: yes
  vars:
    remote_dir: "/home/ubuntu/genre_clf"
    local_files:
      - "/home/ubuntu/genre_clf/server/requirements.txt"
      - "/home/ubuntu/genre_clf/server/app.py"
      - "/home/ubuntu/genre_clf/server/utils/feature_extraction.py"
      - "/home/ubuntu/genre_clf/server/model/min_max_scaler.pkl"
      - "/home/ubuntu/genre_clf/server/model/xgb_mgen.pkl"
      - "/home/ubuntu/genre_clf/server/templates/index.html"
      - "/home/ubuntu/genre_clf/server/templates/result.html"
  tasks:

    - name: Print current directory
      ansible.builtin.shell:
        _raw_params: "pwd"
      register: result

    - name: Ensure the target directory exists
      ansible.builtin.file:
        path: "{{ remote_dir }}"
        state: directory
        owner: ubuntu
        group: ubuntu
        mode: '0755'

    - name: Copy files to remote server
      ansible.builtin.copy:
        src: "{{ item }}"
        dest: "{{ remote_dir }}/"
      loop: "{{ local_files }}"

    - name: Update
      ansible.builtin.shell: 
        cmd: sudo apt update
        
    - name: Install python
      ansible.builtin.shell: 
        cmd: sudo apt install --yes --force-yes python3-pip

    - name: Install all requirement
      ansible.builtin.shell: 
        cmd: pip install -r /home/ubuntu/genre_clf/server/requirements.txt

    - name: Start python file
      ansible.builtin.shell: 
        cmd: python3 ~/genre_clf/server/app.py
