---
- name: Deployment of Genre Classification application
  hosts: all
  become: yes
  vars:
    remote_dir: "/home/ubuntu/genre_clf"
    local_files:
      - "semenistyh_build/server/requirements.txt"
      - "semenistyh_build/server/app.py"
      - "semenistyh_build/server/utils/feature_extraction.py"
      - "semenistyh_build/server/model/min_max_scaler.pkl"
      - "semenistyh_build/server/model/xgb_mgen.pkl"
      - "semenistyh_build/server/templates/index.html"
      - "semenistyh_build/server/templates/result.html"
  tasks:

    - name: Ensure the target directory exists
      ansible.builtin.file:
        path: "{{ remote_dir }}"
        state: directory
        owner: ubuntu
        group: ubuntu
        mode: '0755'

    - name: Check if files exist
      ansible.builtin.stat:
        path: "{{ item }}"
      loop: "{{ local_files }}"
      register: file_check_results

    - name: Fail if any file is missing
      ansible.builtin.fail:
        msg: "File {{ item.item }} is missing!"
      when: not item.stat.exists
      loop: "{{ file_check_results.results }}"

    - name: Copy files to remote server
      ansible.builtin.copy:
        src: "{{ item }}"
        dest: "{{ remote_dir }}/"
      loop: "{{ local_files }}"

    - name: Update
      ansible.builtin.apt:
        update_cache: yes

    - name: Install python
      ansible.builtin.apt:
        name: python3-pip
        force: yes

    - name: Install all requirements
      ansible.builtin.shell:
        cmd: pip install -r {{ remote_dir }}/requirements.txt

    - name: Start python file
      ansible.builtin.shell:
        cmd: python3 {{ remote_dir }}/app.py
